server {
    listen 80;
    server_name koteidevops.duckdns.org;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name koteidevops.duckdns.org;

    ssl_certificate /etc/letsencrypt/live/koteidevops.duckdns.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/koteidevops.duckdns.org/privkey.pem;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}
